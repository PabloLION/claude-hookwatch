/**
 * hookwatch UI entry point.
 * Placeholder — full Preact + htm implementation added in Story 2.2.
 */

const app = document.getElementById("app");
if (app) {
  app.textContent = "hookwatch — loading…";
}
